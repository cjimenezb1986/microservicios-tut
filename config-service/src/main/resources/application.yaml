server:
  port: 8091
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/cjimenezb1986/microservicios-tut
          search-paths: config-data
  application:
    name: config-service
